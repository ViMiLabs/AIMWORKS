PEMFC curated unit registries live here.

These files are derived from local cleaned evidence and reviewed unit decisions, but they do not include the source dataset itself. The release pipeline can apply these curated unit assertions during normal PEMFC builds so the ontology and publication site remain reproducible without pushing the local cleaning workspace.
